<%= form_for @message do |form| %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :message_body %>
  </div>

  <div class="actions">
    <%= hidden_field_tag 'message[chat_room_id]', @chat_room.id %>
    <%= form.submit %>
  </div>
<% end %>
